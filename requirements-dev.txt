-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
locust==2.32.5

black==24.10.0
isort==5.13.2
mypy==1.13.0
flake8==7.1.1
pre-commit==4.0.1

fakeredis==2.26.1

# boto3/sagemaker are NOT here on purpose: sagemaker pins protobuf<5.0, which
# hard-conflicts with opentelemetry-proto's protobuf>=5.0 (both required by
# requirements.txt) -- pip's resolver cannot satisfy both in one environment.
# They're only needed for training/sagemaker_job.py (a lazy import, used
# only when actually launching a SageMaker job), so they live in their own
# file installed just-in-time by that one workflow. See requirements-training.txt.
